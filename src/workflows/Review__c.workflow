<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <rules>
        <fullName>Email Review Alert</fullName>
        <active>false</active>
        <description>When a new review is created, an email is sent to the commentor, requesting that they leave comments about an interviewee.</description>
        <formula>True</formula>
        <triggerType>onCreateOnly</triggerType>
    </rules>
</Workflow>
